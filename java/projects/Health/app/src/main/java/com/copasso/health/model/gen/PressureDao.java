package com.copasso.health.model.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.copasso.health.model.bean.Pressure;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PRESSURE".
*/
public class PressureDao extends AbstractDao<Pressure, Long> {

    public static final String TABLENAME = "PRESSURE";

    /**
     * Properties of entity Pressure.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property SystolicValue = new Property(1, float.class, "systolicValue", false, "SYSTOLIC_VALUE");
        public final static Property DiastolicValue = new Property(2, float.class, "diastolicValue", false, "DIASTOLIC_VALUE");
    }


    public PressureDao(DaoConfig config) {
        super(config);
    }
    
    public PressureDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PRESSURE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"SYSTOLIC_VALUE\" REAL NOT NULL ," + // 1: systolicValue
                "\"DIASTOLIC_VALUE\" REAL NOT NULL );"); // 2: diastolicValue
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PRESSURE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Pressure entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindDouble(2, entity.getSystolicValue());
        stmt.bindDouble(3, entity.getDiastolicValue());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Pressure entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindDouble(2, entity.getSystolicValue());
        stmt.bindDouble(3, entity.getDiastolicValue());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Pressure readEntity(Cursor cursor, int offset) {
        Pressure entity = new Pressure( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getFloat(offset + 1), // systolicValue
            cursor.getFloat(offset + 2) // diastolicValue
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Pressure entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSystolicValue(cursor.getFloat(offset + 1));
        entity.setDiastolicValue(cursor.getFloat(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Pressure entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Pressure entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Pressure entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
